<div class="min-h-screen bg-gradient-to-b from-violet-100 to-white">
  <nav class="p-4">
    <div class="container mx-auto flex justify-between items-center">
      <div class="text-2xl font-bold">
        Drivana
      </div>
      <div id="nav-auth" class="flex items-center gap-4">
        <% if user_signed_in? %>
          <%= button_to destroy_user_session_path, method: :delete, class: "px-4 py-2 border border-gray-300 rounded-lg hover:bg-gray-100 transition-colors duration-200 font-medium" do %>
            Cerrar sesión
          <% end %>
          <%= link_to "Ir al Dashboard", dashboard_path, class: "px-4 py-2 bg-indigo-600 text-white rounded-lg hover:bg-indigo-700 transition-colors duration-200 font-medium" %>
        <% else %>
          <%= link_to "Registrate", new_user_session_path, class: "px-4 py-2 bg-indigo-600 text-white rounded-lg hover:bg-indigo-700 transition-colors duration-200 font-medium" %>
        <% end %>
      </div>
    </div>
  </nav>

  <div class="relative">
    <div class="container mx-auto px-4 pt-16 relative z-10">
      <h1 class="text-6xl font-bold text-indigo-600 mb-4">
        Renta un auto
      </h1>
      <h2 class="text-4xl font-bold text-gray-800 mb-4">
        ¡Saltándote el mostrador para siempre!
      </h2>
      <p class="text-xl text-gray-700 mb-8">
        Valida tu información <span class="text-indigo-600">solo una vez</span> y renta sin complicaciones con un anfitrión local.
      </p>

      <div class="bg-white p-6 rounded-lg shadow-lg max-w-4xl">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          <div class="space-y-2">
            <label class="text-gray-600 mb-2">Lugar</label>
            <input type="text" class="w-full p-3 border rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500" placeholder="¿Dónde quieres recoger?">
          </div>
          <div class="space-y-2">
            <label class="text-gray-600 mb-2">Desde</label>
            <input type="date" class="w-full p-3 border rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500">
          </div>
          <div class="space-y-2">
            <label class="text-gray-600 mb-2">Hasta</label>
            <input type="date" class="w-full p-3 border rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500">
          </div>
        </div>
      </div>
    </div>

    <div class="absolute inset-0 z-0">
      <%= image_tag "banner.jpg", class: "w-full h-screen object-cover" %>
    </div>
  </div>
</div>
